#include <stdio.h>
/**
 * main- entry point
 * Return: 0 no error, non zero if error
 */
int main(void)
{
	char ch;

	putchar("uppercase  character: \n");

	for (ch = 'A'; ch <= 'Z' ch++)

	{
		putchar("%c", ch);
	}
	putchar("\nlowercase characters: \n");

	for (ch = 'a'; ch <= 'z'; ch++);

	{
		putchar("%c", ch);
	}
	return (0);
}
